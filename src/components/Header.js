import NavLinks from './NavLinks';

const Header = () => {
  return (
    <header className="header">
      <div>daniel the developer</div>
      <NavLinks />
    </header>
  );
};

export default Header;
